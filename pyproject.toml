[project]
name = "agentic-web"
version = "0.1.0"
description = "A2A Protocol Agent Demo"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "a2a-sdk[http-server]>=0.0.1",
    "uvicorn>=0.30.0",
]
